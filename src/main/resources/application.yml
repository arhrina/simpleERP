spring:
  profiles:
    active: prod
#  datasource:
#    driver-class-name: org.mariadb.jdbc.Driver
#    url: jdbc:mariadb://192.168.1.25:3306/motok?characterEncoding=UTF-8&serverTimezone=UTC
#    username: root
#    password: 1234

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate: 
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
    logging:
      level: 'org.hibernate.type.descriptor.sql: trace'
    thymeleaf:
      cache: false
    devtools:
      livereload:
        enabled: true
      restart:
        enabled: true

logging:
  level:
    org.hibernate.SQL: debug
